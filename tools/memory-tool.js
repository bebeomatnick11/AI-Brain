const memory=require('../agent/memory'); module.exports={name:'memory',description:'Persistent per-user memory operations.',async execute(args,ctx){const userId=ctx.userId;const op=args.operation||'search';if(op==='remember')return memory.remember(userId,args);if(op==='search')return memory.search(userId,args.query,args.limit||10);if(op==='list')return memory.getAll(userId);if(op==='forget')return memory.forget(userId,args.id);if(op==='clear')return memory.clear(userId);throw new Error('MEMORY_OPERATION_NOT_ALLOWED');}};
